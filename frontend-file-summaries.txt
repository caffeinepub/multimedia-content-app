{
  "frontend/src/App.tsx": "Main application component with TanStack Router setup; adds the /login route pointing to LoginPage alongside the existing Mix, Poetry, Dua, Songs, and Admin routes.",
  "frontend/src/components/Layout.tsx": "Layout wrapper that now treats `/admin` as an unguarded route (alongside `/login`), rendering the admin page with header/footer chrome but skipping the credential check and backend calls — the `PINAuthGuard` component on the admin page handles its own PIN-based authentication independently. All other routes (Mix, Poetry, Dua, Songs) continue to redirect unauthenticated users to `/login` as before.",
  "frontend/src/pages/MixPage.tsx": "Homepage feed that now sorts all posts (Poetry, Dua, Songs) in reverse-chronological order by createdAt and includes a category filter tab bar (All/Poetry/Dua/Songs) with the active tab visually highlighted; the UserIdentityCard and loading/error states are preserved.",
  "frontend/src/pages/PoetryPage.tsx": "Poetry category page with corrected loading/error/empty state logic — uses `isError` (not `error`) for the error branch, checks `poetry === undefined` for the loading guard, and only shows \"No contents available\" when data is a confirmed empty array after a successful fetch.",
  "frontend/src/pages/DuaPage.tsx": "Dua category page with corrected loading/error/empty state logic — uses `isError` for the error branch, checks `duas === undefined` for the loading guard, and only shows \"No duas available\" when data is a confirmed empty array after a successful fetch.",
  "frontend/src/pages/SongsPage.tsx": "Songs category page with corrected loading/error/empty state logic — uses `isError` for the error branch, checks `songs === undefined` for the loading guard, and only shows \"No songs available\" when data is a confirmed empty array after a successful fetch.",
  "frontend/src/pages/AdminPage.tsx": "Admin panel page with PIN guard and three-tab layout (Upload Content, Manage Content, Users & Controls) — the new third tab renders the UsersControlsPanel component for maintenance mode control and user block/unblock management.",
  "frontend/src/components/PINAuthGuard.tsx": "PIN authentication component that validates exact PIN '09186114' and stores authentication state in sessionStorage with error handling for storage operations.",
  "frontend/src/components/PoetryPost.tsx": "Poetry post card with a top-right \"Time Ago\" timestamp label, elegant poetry typography classes, and a bottom action bar featuring a toggleable Like (heart) button with optimistic count update, a Share button using the Web Share API, and a Copy button with a \"Text Copied!\" toast.",
  "frontend/src/components/DuaPost.tsx": "Dua post card with a top-right \"Time Ago\" timestamp label, poetry typography classes, and a bottom action bar featuring Like (heart with optimistic update), Share (Web Share API with clipboard fallback), and Copy (with \"Text Copied!\" toast) buttons.",
  "frontend/src/components/SongPost.tsx": "Song post card with a top-right \"Time Ago\" timestamp label, poetry typography on title/artist, and a bottom action bar featuring Like (heart with optimistic count), Share (Web Share API), and Copy buttons alongside the existing inline play/pause player.",
  "frontend/src/components/AudioPlayer.tsx": "Standalone Dua audio player with local permission check, AudioContext.resume() audio focus request, MediaSession metadata/playbackState management, x-webkit-airplay attribute, volume=1.0/muted=false enforced before every play(), and a ShieldAlert permission denied guard message shown when audio permissions are blocked.",
  "frontend/src/components/admin/PoetryUploadForm.tsx": "Admin form for uploading poetry with title, content, and optional image fields; resets progress state before each submission and shows upload progress using ExternalBlob with trimmed input values.",
  "frontend/src/components/admin/DuaUploadForm.tsx": "Admin form for uploading dua with title, content, and optional audio fields; resets progress state before each submission and shows upload progress using ExternalBlob with trimmed input values.",
  "frontend/src/components/admin/SongUploadForm.tsx": "Admin form for uploading songs with title, artist, and required audio fields; uses explicit comma-separated audio MIME types (audio/mpeg, audio/mp3, audio/wav, audio/ogg, audio/aac, audio/flac, etc.) without any `capture` attribute to ensure the device file manager/music library opens instead of the voice recorder on mobile.",
  "frontend/src/components/admin/ContentManagement.tsx": "Admin content management panel listing all poetry, dua, and song entries with per-item delete buttons and a shared confirmation dialog — updated `ContentItem` type and like count display to use `likeCount: bigint` instead of the old `likes: { count: bigint }` shape.",
  "frontend/src/hooks/useQueries.ts": "React Query hooks for all backend operations — admin-only operations (getAllUsers, setMaintenanceMode, blockUser, unblockUser, createPoetry, createDua, createSong, deletePoetry, deleteDua, deleteSong) now use useAdminActor instead of useActor so they are authorized even without Internet Identity login.",
  "frontend/src/index.css": "Global styles with the existing OKLCH color palette; adds a Google Fonts import for Lora (elegant serif) and defines two new utility classes — `.poetry-title` (Nastaliq/Lora serif, 1.5 line-height) and `.poetry-content` (Nastaliq/Lora serif, 1.85 line-height) — for consistent, readable poetry typography across all post cards.",
  "frontend/tailwind.config.js": "Tailwind configuration with custom purple-tinted color palette, Urdu font family, and extended design tokens.",
  "frontend/index.html": "HTML entry point with updated title \"Dard-e-munasif\" and favicon pointing to the custom circular logo image.",
  "frontend/src/utils/indexedDB.ts": "IndexedDB utility module that initializes a database with 'history' and 'downloads' object stores, providing async functions for adding/retrieving history entries, managing audio blob downloads, and creating blob URLs for offline playback.",
  "frontend/src/hooks/useHistory.ts": "React hook that manages post/song view history using IndexedDB, providing addHistoryEntry and refreshHistory functions along with the current history array state.",
  "frontend/src/hooks/useDownloads.ts": "React hook that manages downloaded audio files in IndexedDB, providing addDownload, deleteDownload, playDownload, and refreshDownloads functions along with the current downloads array state.",
  "frontend/src/hooks/useWebAudioFilters.ts": "Stub/no-op hook kept only for import compatibility — the entire Web Audio filter chain has been bypassed to fix silent audio; all methods are no-ops and the hook returns fixed 'standard' mode without any state or AudioContext usage.",
  "frontend/src/hooks/useAudioPlayback.ts": "Centralized audio playback hook with Web Permissions API check (exposes `isPermissionDenied` flag), AudioContext.resume() audio focus request before every play(), MediaSession metadata/playbackState management for OS media stream routing, x-webkit-airplay attribute on the singleton HTMLAudioElement, and unconditional volume=1.0/muted=false enforcement before every play call.",
  "frontend/src/components/FullScreenPostModal.tsx": "Full-screen overlay modal for Poetry and Dua posts showing full title, image/audio, content, and like count — updated to use `post.likeCount` (bigint) instead of the removed `post.likes.count` shape to match the current backend interface.",
  "frontend/src/components/NowPlayingModal.tsx": "Full-screen Now Playing modal that consumes `isPermissionDenied` from useAudioPlayback; skips auto-play when permissions are denied, disables the play button, hides the seeker bar, and shows a ShieldAlert permission guard message in place of the progress controls.",
  "frontend/src/components/HamburgerMenu.tsx": "Hamburger menu with History and Downloads sections only — the entire Volume Manager section (master volume slider and filter mode toggles) has been removed; downloaded audio playback also enforces volume=1.0 and muted=false.",
  "frontend/src/pages/LoginPage.tsx": "Login page with two-step flow (name → server selection) that now generates a stable browser fingerprint device ID, stores it in localStorage under 'dmUser_deviceId', calls the backend registerUser mutation on server selection, stores the returned uniqueCode in localStorage, and shows an inline error if registration fails.",
  "frontend/src/components/BanScreen.tsx": "Full-screen ban/suspension screen with a ShieldX icon, \"Account Suspended\" title, support message, and a prominent green \"Contact Admin\" button that opens WhatsApp (wa.me/919541525891) with the pre-filled message \"Blocked.API.9.0\" in a new tab.",
  "frontend/src/components/MaintenanceScreen.tsx": "Full-screen maintenance screen that fills the entire viewport with an animated Wrench icon and the exact message \"App is currently under maintenance for a better experience. We will be back online shortly – Developer Ikhlas.\" with bouncing dots animation.",
  "frontend/src/components/admin/UsersControlsPanel.tsx": "Admin panel component with Maintenance Mode toggle (optimistic local state, spins while updating) and user management table with block/unblock confirmation dialogs — error states show only plain user-friendly messages, the Retry button calls refetchUsers(), and all error toasts display clean messages without raw IC error details.",
  "frontend/src/utils/timeAgo.ts": "Utility function that converts an IC nanosecond bigint timestamp to a human-readable relative time string (e.g., \"Just Now\", \"2 hours ago\"), returning an empty string for zero/null values.",
  "frontend/src/hooks/useAdminActor.ts": "Hook that initializes a backend actor with the admin token unconditionally (no Internet Identity required) using `createActorWithConfig` and `getSecretParameter('caffeineAdminToken')`, matching the same pattern as `useActor.ts`; uses a module-level singleton to avoid re-initialization."
}