# Specification

## Summary
**Goal:** Rename the app from "Spiritual Hub" to "Dard-e-munasif" and replace the branding logo with the uploaded black & white circular image across the frontend.

**Planned changes:**
- Update the browser tab title (`<title>`) to "Dard-e-munasif"
- Update the navbar/header app name text to "Dard-e-munasif"
- Replace any remaining visible "Spiritual Hub" text in the UI
- Use the edited circular logo (`dard-e-munasif-logo.dim_200x200.png`) in the navbar/header next to the app name
- Set the same logo as the browser favicon

**User-visible outcome:** The app displays "Dard-e-munasif" as its name everywhere, with the black & white blindfolded figure logo shown in the header and browser tab.
